version: 2.1
orbs:
    heroku: circleci/heroku@1.2.6
    cypress: cypress-io/cypress@1
workflows:
    heroku_deploy:
        jobs:
            - cypress/run
            - heroku/deploy-via-git:
                  requires:
                      - cypress/run
